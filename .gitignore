# =============================================================================
# trading-research — .gitignore
# High-fidelity: excludes data, logs, venvs, caches; preserves directory skeleton.
# =============================================================================

# -----------------------------------------------------------------------------
# Python runtime / build artifacts
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
*.pyd
.Python
.venv/
venv/
env/
ENV/
*.egg-info/
*.egg
build/
dist/
pip-wheel-metadata/
pip-log.txt
pip-delete-this-directory.txt

# -----------------------------------------------------------------------------
# uv / package managers
# -----------------------------------------------------------------------------
.uv/
# uv.lock IS committed — do not ignore it.

# -----------------------------------------------------------------------------
# Test / lint / type-check caches
# -----------------------------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/
.pytype/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
htmlcov/
.hypothesis/

# -----------------------------------------------------------------------------
# Jupyter / notebook artifacts
# -----------------------------------------------------------------------------
.ipynb_checkpoints/
notebooks/*.ipynb
notebooks/scratch/
!notebooks/.gitkeep

# -----------------------------------------------------------------------------
# Project data layers (RAW / CLEAN / FEATURES) — exclude all bulk payloads,
# keep the directory skeleton and manifests/quality sidecars that describe
# what *should* be there.
# -----------------------------------------------------------------------------
data/raw/**
data/clean/**
data/features/**

# Preserve layer directories and the dotfiles that mark them
!data/
!data/raw/
!data/clean/
!data/features/
!data/raw/.gitkeep
!data/clean/.gitkeep
!data/features/.gitkeep

# Preserve manifest + quality sidecars so the layer state is describable
# without shipping the payloads.
!data/raw/**/*.manifest.json
!data/clean/**/*.manifest.json
!data/features/**/*.manifest.json
!data/raw/**/*.quality.json
!data/clean/**/*.quality.json

# Preserve `known_outages.yaml` and any other YAML catalog under data/
!data/**/*.yaml
!data/**/*.yml

# -----------------------------------------------------------------------------
# Backtest / run outputs — exclude all generated artefacts, preserve skeleton
# and the trials registry (needed for cross-session deflated Sharpe auditing)
# -----------------------------------------------------------------------------
runs/**
!runs/
!runs/.gitkeep
# trials.json is the audit trail for DSR: how many variants were tested.
# Tracked so that deflated-Sharpe claims can be verified across sessions.
!runs/.trials.json

# -----------------------------------------------------------------------------
# Migration artifacts (generated by prep_migration_samples.py — small, but
# regenerable and host-specific, so do not commit)
# -----------------------------------------------------------------------------
migration_samples/

# -----------------------------------------------------------------------------
# Logs
# -----------------------------------------------------------------------------
*.log
logs/
outputs/logs/
*.log.*
# Keep session work-logs — they are project memory, not runtime logs.
!outputs/work-log/
!outputs/work-log/**/*.md

# -----------------------------------------------------------------------------
# Reports / large HTML exports (generated, regenerable from runs/)
# -----------------------------------------------------------------------------
outputs/reports/
!outputs/reports/.gitkeep

# -----------------------------------------------------------------------------
# Validation outputs — audit artefacts are canonical; HTML snapshots are not
# outputs/validation/*.md are the audit records and ARE committed.
# Any generated HTML or JSON dumps inside validation/ are not.
# -----------------------------------------------------------------------------
outputs/validation/**
!outputs/validation/
!outputs/validation/*.md
!outputs/validation/session-14-evidence/
!outputs/validation/session-14-evidence/*.txt

# -----------------------------------------------------------------------------
# Legacy content — preserve scaffold only, not imported code trees
# -----------------------------------------------------------------------------
Legacy/**/__pycache__/
Legacy/**/.venv/
Legacy/**/*.parquet
Legacy/**/*.csv
Legacy/**/*.log

# -----------------------------------------------------------------------------
# Secrets / credentials — never commit
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
configs/secrets.yaml
configs/secrets.*.yaml
*.pem
*.key
*_credentials.json
*_token.json
tradestation_tokens.json

# -----------------------------------------------------------------------------
# OS / editor / tooling
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.vscode/
.idea/
*.swp
*.swo
*~
.claude/local/
# settings.local.json is machine-specific and should not be shared
.claude/settings.local.json
.antigravity/

# -----------------------------------------------------------------------------
# Binary / archive / bulk payloads that should never be in git
# -----------------------------------------------------------------------------
*.parquet
*.feather
*.arrow
*.h5
*.hdf5
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.rar

# Allow small fixture parquets under tests/ (explicit opt-in)
!tests/**/*.parquet
