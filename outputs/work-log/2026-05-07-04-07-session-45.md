# Session Summary — 2026-05-07 04:07

## Completed

- Authored Chapter 14 — The Backtest Engine (~6 pages, Opus-style teaching prose)
- Authored Chapter 15 — Trade Schema & Forensics (~4 pages, reference)
- Authored Chapter 16 — Running a Single Backtest (~4 pages, reference)
- Authored Chapter 18 — The Replay App (~3 pages, reference)
- Full test suite: exit code 0, no new failures

## Files changed

- `docs/manual/14-backtest-engine.md` — new; covers engine design (bar-by-bar walk, auditability rationale), fill models (NEXT_BAR_OPEN vs SAME_BAR with justification guard), cost model (slippage ticks + commission USD with instrument defaults and per-config overrides), TP/SL pessimistic resolution and OFI opt-in, EOD flat logic and rationale, max_holding_bars semantics (null vs large number), the Mulligan controller (Rules M-1 M-2 M-3, combined_risk, MulliganViolation), and what the engine explicitly does not do (no portfolio limits, no daily loss limit, no broker reconciliation)
- `docs/manual/15-trade-schema.md` — new; covers TRADE_SCHEMA fields in full (pyarrow schema, all 21 fields), the two entry timestamp pairs and two exit timestamp pairs and why, all five exit reasons with trigger logic and fill-bar semantics, MAE/MFE sign conventions and what they tell you about stop placement and exit discipline, monetary field formulas, and a worked guide to first-look forensics on a raw trade log
- `docs/manual/16-running-a-backtest.md` — new; covers the `backtest` CLI in full (all options, three dispatch paths, date filters, exit codes), all three output artefacts (trades.parquet, equity_curve.parquet, summary.json with CI format), annotated summary table reading guide (CI interpretation, Sharpe vs Sortino consistency), trial registry side-effect (what records, modes, the append-only accumulation), and a consolidated common-error table with resolutions
- `docs/manual/18-replay-app.md` — new; covers what the 2×2 chart grid shows (trade markers, OFI subplot), the `replay` CLI (all options with defaults, data requirements), two distinct use modes (pre-backtest sanity vs post-backtest forensics, including the chart-bias caveat), and full layout reference (date-picker, timeframe focus, Plotly chart interactions)

## Decisions made

- Chapter 15 documents `exit_reason` as five values (target, stop, signal, eod, time_limit). The TOC description included "mulligan" as a sixth exit reason but the code does not implement it — `MulliganViolation` is a rejected scale-in guard, not an exit. Documented what the code actually does. The TOC description was aspirational; the chapter is the authoritative reference.
- Ruff violations in `fills.py` (UP042, two RET505) are pre-existing and not introduced this session. Noted but not fixed — standing rule is no new violations, not zero violations in untouched files.
- Chapter 14 adopts the mentor's framing on Mulligan (legitimate technique vs averaging down) because that framing is load-bearing for understanding the M-1/M-2/M-3 rules. The code and the chapter are consistent.

## Next session starts from

- Part IV is complete except Chapter 17 (Trader's Desk Report), which is bundled with report-rigor-surfacing code in session 46.
- Session 46 spec: Chapter 17 + code changes to surface DSR, fold variance, and CI flags in the report header (GAP items §17.5, §22.7, §23.5 in the TOC).
- No manual gap items were closed this session — the four chapters authored were already [EXISTS] stubs in the TOC. They now have content at quality bar.
